{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Casa creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('casa') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
